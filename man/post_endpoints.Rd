% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/endpoints.asentamientos.R,
%   R/endpoints.calles.R, R/endpoints.departamentos.R,
%   R/endpoints.direcciones.R, R/endpoints.establecimientos_educativos.R,
%   R/endpoints.fracciones_censales.R, R/endpoints.gobiernos_locales.R,
%   R/endpoints.instituciones_universitarias.R, R/endpoints.localidades.R,
%   R/endpoints.localidades_censales.R, R/endpoints.municipios.R,
%   R/endpoints.provincias.R, R/endpoints.radios_censales.R,
%   R/endpoints.ubicacion.R, R/post_endpoints.R
\name{post_asentamientos}
\alias{post_asentamientos}
\alias{post_calles}
\alias{post_departamentos}
\alias{post_normalizar_direccion}
\alias{post_establecimientos_educativos}
\alias{post_fracciones_censales}
\alias{post_gobiernos_locales}
\alias{post_instituciones_universitarias}
\alias{post_localidades}
\alias{post_localidades_censales}
\alias{post_municipios}
\alias{post_provincias}
\alias{post_radios_censales}
\alias{post_ubicacion}
\alias{post_endpoints}
\title{Consultas POST por lotes a la georef-ar-api}
\usage{
post_asentamientos(queries_list)

post_calles(queries_list)

post_departamentos(queries_list)

post_normalizar_direccion(queries_list)

post_establecimientos_educativos(queries_list)

post_fracciones_censales(queries_list)

post_gobiernos_locales(queries_list)

post_instituciones_universitarias(queries_list)

post_localidades(queries_list)

post_localidades_censales(queries_list)

post_municipios(queries_list)

post_provincias(queries_list)

post_radios_censales(queries_list)

post_ubicacion(queries_list)
}
\arguments{
\item{queries_list}{Lista de listas. Cada elemento es una consulta expresada
como una lista nombrada, cuyos nombres se corresponden con los parámetros
aceptados por la función \code{get_*} del mismo endpoint (por ejemplo
\code{nombre}, \code{id}, \code{max}, \code{campos}).}
}
\value{
Un tibble con los resultados combinados de todas las consultas
enviadas. Si ninguna consulta produce resultados, se devuelve un tibble
vacío y se emite un \code{warning}.
}
\description{
Familia de funciones que permiten realizar múltiples consultas a un mismo
endpoint de la georef-ar-api en una sola llamada, utilizando el método POST.
A diferencia de las funciones \code{get_*}, que resuelven una consulta por
vez, las funciones \code{post_*} reciben un conjunto de consultas, las
agrupan en lotes según los límites de la API, las envían en paralelo y
combinan las respuestas en un único resultado.
}
\details{
Cada función corresponde al endpoint homónimo y acepta los mismos parámetros
de búsqueda que su equivalente \code{get_*}. Para aumentar la cuota de uso,
se puede configurar un token JWT en la variable de entorno
\code{GEOREFAR_TOKEN} (ver
\url{https://datosgobar.github.io/georef-ar-api/jwt-token/}).
}
\examples{
\dontrun{
consultas <- list(
  list(nombre = "Neuquen"),
  list(nombre = "Santa Fe", max = 1)
)
post_provincias(consultas)

post_ubicacion(list(
  list(lat = -34.6037, lon = -58.3816),
  list(lat = -31.4201, lon = -64.1888)
))
}

}
\references{
\href{https://datosgobar.github.io/georef-ar-api/}{georef-ar-api}
}
